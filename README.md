Build a dashboard to explore the client’s telemetry data

The client’s telemetry data was converted from JSON to CSV format using Python to enable efficient analysis and visualization in Tableau.

Input File: daikibo-telemetry-data.json

Output File: daikibo_telemetry.csv

The processed CSV file serves as the primary data source for building interactive Tableau dashboards and deriving meaningful insights from the telemetry data.
